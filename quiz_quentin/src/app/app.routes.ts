import { Routes } from '@angular/router';
import { questionnaireComponents } from './questionnaire/questionnaire.component';

export const routes: Routes = [
    {path:'questionnaire', component: questionnaireComponents},
];
